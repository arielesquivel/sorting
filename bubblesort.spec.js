describe("Bubble Sort", function () {
  it("handles an empty array", function () {
    expect(bubbleSort([1, 5, 6, 2, 3, 9])).toEqual([1, 2, 3, 5, 6, 9]);
  });
});
describe("array vacio", function () {
  it("handles an empty array", function () {
    expect(bubbleSort([])).toEqual([]);
  });
});
describe("Bubble Sort", function () {
  it("handles an empty array", function () {
    expect(bubbleSort([1, 6, 8, 2, 3, 9])).toEqual([1, 2, 3, 6, 8, 9]);
  });
});
describe("array vacio", function () {
  it("handles an empty array", function () {
    expect(bubbleSort([])).toEqual([]);
  });
});
describe("Bubble Sort", function () {
  it("handles an empty array", function () {
    expect(bubbleSort([1, 7, 8, 4, 3, 9])).toEqual([1, 3, 4, 7, 8, 9]);
  });
});
describe("array vacio", function () {
  it("handles an empty array", function () {
    expect(bubbleSort([])).toEqual([]);
  });
});
